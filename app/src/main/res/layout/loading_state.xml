<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="com.example.mvvmarchitectureexample.repository.Resource" />
        <variable
            name="resource"
            type="Resource" />
        <variable
            name="callback"
            type="com.example.mvvmarchitectureexample.ui.common.RetryCall" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:visibleGone="@{resource.data == null}"
        android:gravity="center"
        android:padding="@dimen/default_margin">
        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:visibleGone="@{resource.status == com.example.mvvmarchitectureexample.repository.Status.LOADING}"
            style="?android:attr/progressBarStyle"
            android:layout_margin="8dp" />

        <Button
            android:id="@+id/btn_retry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/retry"
            android:onClick="@{() -> callback.retry()}"
            app:visibleGone="@{resource.status == com.example.mvvmarchitectureexample.repository.Status.ERROR}" />

        <TextView
            android:id="@+id/tv_error"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{resource.message ?? @string/unknown_error"
            app:visibleGone="@{resource.status == com.example.mvvmarchitectureexample.repository.Status.ERROR}" />
    </LinearLayout>
</layout>